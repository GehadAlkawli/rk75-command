# JustRunMy public RK75 host

Deploy this small Node service to JustRunMy.App to serve the full RK75 Command application under an independent public address. It forwards all pages and API calls, so sessions, member statistics, and admin features continue to work.
